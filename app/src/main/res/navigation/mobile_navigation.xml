<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/fragSchede">
    <fragment
        android:id="@+id/fragTimer"
        android:name="com.example.trainingproject.Timer"
        android:label="fragment_timer"
        tools:layout="@layout/fragment_timer" />
    <fragment
        android:id="@+id/fragSchede"
        android:name="com.example.trainingproject.Schede"
        android:label="fragment_schede"
        tools:layout="@layout/fragment_schede" />
    <fragment
        android:id="@+id/fragCronometro"
        android:name="com.example.trainingproject.Cronometro"
        android:label="fragment_cronometro"
        tools:layout="@layout/fragment_cronometro" />
</navigation>